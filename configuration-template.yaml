# https://www.home-assistant.io/integrations/default_config/
# default_config:

# Add needed setup for the integraiton,
assist_pipeline:
cloud:
config:
conversation:
homeassistant_alerts:
logbook:
stream:
webhook:

# comment out history to test if missing, water events disabled.
history:

# Enable to initiate debugging in Home Assistant Runtime
debugpy:
  start: false
  wait: false

# https://www.home-assistant.io/integrations/logger/
logger:
  default: warning
  logs:
    custom_components.mastertherm: debug
    masterthermconnect: debug
